// This file previously contained the Next.js root layout.
// It is now unused because the app runs as a plain React SPA via Vite.
// We keep an empty component here only so any stale imports compile safely.

export default function RootLayout() {
  return null;
}
